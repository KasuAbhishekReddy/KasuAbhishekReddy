name: Generate contribution graphs

on:
  schedule:
    # Every 12 hours
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Generate snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            snake-out/github-snake.svg
            snake-out/github-snake-dark.svg?palette=github-dark

      - name: Generate pacman
        uses: abozanona/pacman-contribution-graph@main
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            pacman-out/pacman-contribution-graph.svg
            pacman-out/pacman-contribution-graph-dark.svg?gameTheme=github-dark

      - name: Collect outputs
        run: |
          sudo mkdir -p dist
          sudo cp -r snake-out/. dist/
          sudo cp -r pacman-out/. dist/
          sudo chown -R "$(id -u):$(id -g)" dist
          ls -la dist

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
